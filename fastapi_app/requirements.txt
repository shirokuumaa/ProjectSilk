# --- Core web server ---
fastapi==0.121.2
uvicorn[standard]==0.38.0

# --- Uploads (multipart/form-data) ---
python-multipart

# --- Image processing / background removal ---
pillow
rembg==2.0.61

# rembg пока ещё не дружит с numpy 2.x
numpy<2

# rembg использует onnxruntime (обязательно!)
onnxruntime==1.19.2

# --- 3D / mesh utils (для наших OBJ/GLB файлов) ---
trimesh==4.9.0
